services:
  debate:
    build: .
    stdin_open: true
    tty: true
    env_file: .env
    volumes:
      - ./maverickj:/app/maverickj
      - ./config.yaml:/app/config.yaml:ro
      - ./reports:/app/reports
